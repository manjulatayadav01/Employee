import { MultiSearchFilterPipe } from './multi-search-filter.pipe';

describe('MultiSearchFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MultiSearchFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
